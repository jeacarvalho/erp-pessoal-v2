[tool:pytest]
testpaths = backend/tests web/tests
addopts = -v --tb=short --strict-markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    e2e: marks tests as end-to-end tests
    frontend: marks tests as frontend tests

[coverage:run]
source = 
    backend/app
omit = 
    */tests/*
    */__pycache__/*
    */venv/*
    */env/*
    */.venv/*
    */app_streamlit.py
    web/app/

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    pass
    except ImportError:

# Cobertura mínima exigida para backend
fail_under = 70

[coverage:html]
directory = coverage_html

[coverage:xml]
output = coverage.xml